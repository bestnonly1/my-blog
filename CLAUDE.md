# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 참고하는 가이드입니다.

## 프로젝트 개요

마크다운(.md) 파일을 읽어 정적 블로그 웹사이트로 변환하는 프로젝트입니다. 별도의 프레임워크(React, Vue, Next.js 등)나 정적 사이트 생성기(Jekyll, Hugo 등)를 쓰지 않고, 순수 HTML/CSS/JavaScript만으로 구현합니다.

## 핵심 요구사항

- **입력**: `posts/` 같은 디렉터리에 있는 마크다운 파일들
- **출력**: 읽기 좋은 블로그 웹사이트 (글 목록 페이지 + 개별 글 페이지)
- **프레임워크 없음**: 빌드 도구, 번들러, npm 패키지 의존을 최소화하고 바닐라 JS로 구현. 마크다운 파싱도 외부 라이브러리보다는 직접 구현하거나, 꼭 필요하면 CDN 없이 로컬에 벤더링한 경량 파서 하나 정도만 허용
- **다크 모드**: `prefers-color-scheme` 기반 자동 감지 + 사용자가 직접 토글할 수 있는 스위치. 선택은 `localStorage`에 저장
- **반응형**: 모바일에서도 레이아웃이 깨지지 않고 읽기 편해야 함 (가변 폭, 적절한 폰트 크기, 터치하기 쉬운 네비게이션)
- **디자인 톤**: 미니멀하고 깔끔하게. 본문 가독성(줄 간격, 본문 폭, 타이포그래피)을 최우선으로 고려

## 아키텍처 방향

- 마크다운 → HTML 변환은 빌드 타임(정적 생성) 또는 런타임(클라이언트에서 fetch 후 파싱) 중 하나를 선택해야 함. 별도 결정이 없다면 **빌드 타임 변환**을 기본으로 한다 (SEO, 초기 로딩 속도에 유리).
- 글 메타데이터(제목, 날짜, 태그 등)는 마크다운 파일 상단의 front matter(YAML)로 관리
- 페이지 구성 예시:
  - `index.html` — 글 목록
  - `posts/*.html` (빌드 결과물) 또는 `post.html?slug=...` (런타임 방식일 경우)
  - `css/style.css` — 라이트/다크 테마 변수 기반 스타일시트
  - `js/` — 다크모드 토글, (런타임 방식이면) 마크다운 렌더링 로직

## 코딩 컨벤션

- 불필요한 추상화나 설정 옵션을 미리 만들지 않는다 — 필요할 때 추가
- CSS는 커스텀 프로퍼티(CSS 변수)로 라이트/다크 테마 값을 정의하고 `[data-theme]` 또는 `prefers-color-scheme`로 전환
- JS는 모듈 단위로 작게 나누되, 과도한 파일 분리는 지양 (바닐라 JS 프로젝트이므로 단순하게 유지)
- 주석은 "왜"가 명확하지 않은 부분에만 최소한으로 작성

## 참고

- 아직 초기 단계 프로젝트로, 코드베이스가 비어 있음. 구현을 시작할 때는 먼저 디렉터리 구조와 첫 글 1~2개로 프로토타입을 만들고, 디자인이 확정되면 확장하는 방식을 권장
